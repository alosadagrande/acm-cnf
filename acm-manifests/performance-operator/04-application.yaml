apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: performance-addon
  namespace: openshift-performance-addon
spec:
  componentKinds:
  - group: apps.open-cluster-management.io
    kind: Subscription
  descriptor: {}
  selector:
    matchExpressions:
    - key: app
      operator: In
      values:
      - acm-performance-addon
      - acm-performance-addon-cnf20
