apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: placement-ptp
  namespace: acm-cnf-ptp
spec:
  clusterConditions:
   - type: OK
  clusterSelector:
    matchExpressions: []
    matchLabels:
      ptp: "true"
